name: test
on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: test-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validation-retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Validation PR retired; canonical qualification runs on PR #8."
