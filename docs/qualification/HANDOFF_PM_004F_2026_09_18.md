# DiffWitness — PM-004f handoff

Read https://github.com/TFourniax/tooltest/issues/74 for exact refs, workflow results
and failures. Global NOT ALPHA READY; HUMAN NOT RUN. Core#103/#104 are complete
bounded units. PM-004f extends the same extraction registry, transport and Git-tree
index with optional JS/TS/JSX/TSX syntax. Python contract/IDs remain unchanged.
No second store, runtime installation, source execution or new authority.

Acceptance/limits: PM_004F.md and STRUCTURE_EXTRACTION.md. Initial missing-provider
failures and native callback crash are preserved in evidence/PM_004F*. Do not use
Python progress callbacks: that upstream path crashes on Python3.12. The actual
pinned0.25.2 adapter uses the retained native timeout; its deprecated API/pin
requires fresh qualification before changes. Missing/incompatible grammar,
invalid syntax/UTF-8 and exceeded limits give empty/unparsed facts.

Existing21 Core jobs plus3 installed optional-provider jobs(24 total), all three
companion workflows, review and fresh exact-main qualification are mandatory.
Local full suites with/without parsers,26 structural tests, installed wheels,
10k benchmark and unchanged IdleProof Python budgets are recorded. Exact remote
PASS must come from #74, not inferred from local results.

Next: remaining languages and consumers, longitudinal identity/history, drift,
cited FR/EN context, why/history and impact, multi-writer, Portal storage/UI/cited
answers, explicit user memory, PM018, exact candidate human journeys, #80.
Keep every full PM scope; persisted-safe validation and large-history failures
stay open. No tag/release early. Qualified merges authorized, fresh main after
 each, report/handoff closure and append-only failure history required.

Linux work/core_syntax uses separate venv_syntax and venv_syntax_base editables;
consumer_pm004f is an isolated installed wheel. Preserve old checkouts and read
remote/dirty state before mutation. No subagents used.
