# PM012f — selected extraction startup after PM005a candidate failure

Acceptance before implementation,2026-09-20. PR114 exactheadc9e83e7,
tree9c0c318e, fails Python actual hookp95188.8ms>150ms in test35521285219,
job106105705280. Installed syntax and feature smoke pass; subsequent TS/JSON
hooks are not reached. Main28e2fc5 stays qualified and unchanged. No rerun.

One diagnostic run per installed parent/candidate, using a Node preload that
delegates unchanged real subprocess calls: parent hookp95104.3msPASS, candidate
FAIL. Parent43extractions(including reached Stop/coverage) median72.8/p95103.0ms;
candidate40extractions(stopped at hook assertion) median75.8/p95195.4ms.
Other Git subprocesses are also slower in the candidate run. Sequential rather
than interleaved measurements do not establish a causal source regression or
identify the failed CI runner's scheduling cause. Preserve raw calls and failures.

Confirmed unnecessary initialization: every extraction constructs unrelated
Project State command parsers, now including lineage; every language eagerly
loads both Python and optional syntax adapter implementations. Remove those
dependencies with the existing single parser definitions and canonical registry.
Move the one shared grammar catalogue into a lightweight module, retaining all
existing aliases, pinned versions, wrapper injection seams, admission and exact
provider behavior. Python need not initialize optional syntax; optional syntax
need not initialize Python AST extraction; unsupported files need neither.

Fresh-subprocess isolation must fail before and pass after, preserving v1/v2,
FR/EN,required --json,invalid arguments,strict JSON,bounds and source-byte truth.
No source/results cache,daemon,alternative parser,changed grammar checks or
relaxed threshold. Measure fixed alternating pairs with byte-identical responses,
then full/base,compile,wheel/installed,10k,exact24+3/review and freshmain. A new
passing source can qualify only this bounded improvement; it never erases the
original failed PR/main attempts or proves universal latency. FullPM012 remains
open. Integrate this repair with the already blocked PM005a candidate.

## Source isolation and measurements

Strengthened10transport tests expose10failing subcases before the correction:
four unrelated command-parser initializations, two Python optional-adapter
initializations and four unselected-provider initializations. After correction,
all10transport tests PASS2.473s and all85structure tests PASS3.887s. The parser
uses the same extract argument declaration and handler, with --json still
required; unrelated subparsers are constructed for their own commands and
complete state help. The shared catalogue is moved verbatim; callable wrappers
preserve existing injection seams and select the existing provider implementation.

Alternating20installed before/after pairs per language, exact response bytes
equal with actual parsed providers: medianPython76.440→72.547ms,
TS105.387→102.113ms,JSON103.982→102.212ms. All p95 values are worse in this
sample:Python122.550→143.638,TS158.123→165.383,JSON132.035→137.426ms.
Do not claim improved tail latency or a proven fix for the failed CI runner.

One subsequent unchanged actual-hook gate per family, sequential and without
other local tests:Python90.8ms,TS114.0ms,JSON143.7ms p95 PASS; maxima
107.0/120.2/236.5ms and Stop222.6/241.3/428.1ms also pass unchanged bounds.
These local successes do not erase any earlier failure or qualify remote source.
Original PR114 test35521285219 finishes23PASS/1FAIL; three auxiliary workflows
pass and automated review5750908408 completes without findings. It remains
PR_FAILED. Exact original run/attempt/artifact metadata is retained.

New wheelSHA25682fff4c3c46f44e227ecb645a1590da8287c21c2652b186867630d2020e970fc;
all99Python wheel and installed files match source bytes. Full/base and installed
qualification are running on that frozen source; no full qualification claimed
at this checkpoint.

Final frozen-source gates:651full/647PASS/4platform skips125.510s;
651base/599PASS/52explicit skips125.395s. Compile,wheel/pip,installed syntax,
Node transport,all-branch history,cited lineage FR/EN and integrated product
journey PASS. Sequential10k append1.202668s,verify.519612s,rebuild1.189058s,
cold32.996ms,hotp9534.564ms PASS. Source/log hashes in PM_012F_local_manifest.
Original c9 PR remains FAILED; new corrected-head and freshmain qualification
must independently pass. No threshold change or result cache was introduced.
