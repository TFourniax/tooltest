# PM-004m — source-aware imports through the shared contract

Acceptance before implementation,2026-09-19. Development starts at K d278c89;
publication waits for qualified K main. Current J/Windows gates remain separate
and blocking. Global NOT ALPHA READY; MACHINE only, HUMAN NOT RUN.

Actual discovery: pkg/__init__.py `from .child import run` becomes bare child;
`from . import sibling` loses its package reference; root/out-of-root relative
imports lose leading dots and can be associated with unrelated global names.
Fix package-aware lexical bases and preserve unresolved raw relative references.
No runtime imports, source execution, sys.path/manifest resolution or namespace
package discovery. A source layout is a bounded static model, never Proof.

Extend StructuralImport in the existing provider-neutral contract with source
line/end_line, raw source target and imported member names. Python members are
observed syntax, not automatically modules. All actual language adapters should
retain import source positions; unavailable detail must remain explicitly null,
never fabricated. Static local component resolution is INFERRED, unresolved
references stay neutral; do not assert third-party ownership from absence in the
bounded file index. Ambiguous same-module candidates must not choose one target.

Evolve the existing byte transport with explicit opt-in request/response v2.
Default v1 retains its exact field shape for current strict IdleProof consumers;
both versions reuse one extraction/admission, not separate extractors. v2 exposes
import metadata and explicit file extraction schema2. Old schema rejection and
malformed/duplicate JSON/path/hash/authority/source limits remain enforced.
Refresh provider profile8. Existing dependency-free and optional fallback remain.

Before code: regress package initializers, valid/out-of-root relative bases,
aliases/multiline/from-member imports, source positions/CRLF/Unicode, comments and
strings, same-name module/package ambiguity, v1 compatibility and v2 boundaries.
Verify immutable Git index uses only unique local candidates and retains raw
unresolved refs without an external-owner claim. Actual installed FR/EN protocol,
full/base suite, optional3OS24+3 gates, review and fresh main are required.
Budgets/timeouts unchanged; all old failures retained.

Following consumer/feature-map work must request/admit the richer contract and
preserve source citations. This unit does not itself close graph adoption,
routes/tables/technology inference or the complete PM004/longitudinal vision.
Primary Python import reference: https://docs.python.org/3/reference/import.html .

## Implementation, review and preserved failures

Actual initial seven methods yield5failures/17errors (subtests) before code. A
first harness invocation used a wrong cwd and discovered zero tests; it is not
fail-before product evidence. A later edit helper repeated the wrong relative
path and failed before mutation; its partial targeted replay is historical.
Corrected absolute-path edit applies positions through the single syntax adapter
callback, package-aware Python bases and neutral/unique inferred index targets.
Seven targeted and72 structural tests PASS; first full609/605PASS/4skips58.383s,
base609/566PASS/43skips56.938s, installed v1/v2 FR/EN and pip check PASS.

Local review adds a regression for unavailable member detail: default empty tuple
can be mistaken for observed absence in non-Python providers. Eight actual-language
subtests FAIL before changing unavailable members to null. Python import explicitly
uses an empty member tuple; from-import retains observed names. Source positions
exist for each implemented grammar, while non-Python source_target/member detail
remains explicitly null. New full/base/wheel/installed/budget qualification runs
after that correction; initial green runs do not qualify the final source.

V1 byte response field shape remains unchanged; v2 is opt-in and uses the same
extraction/admission. The current L strict v1 consumer passed its actual9-family
smoke against the initial M wheel. Final replay required after null-detail change.
Unresolved graph targets are module-reference, never third-party ownership. Local
Python graph links are INFERRED and ambiguous module/package layouts stay unbound.
No source execution/sys.path discovery. Imported members are not automatically
module targets. CommonJS/dynamic-import coverage and full feature-map surfaces
still need their explicit follow-up; no complete PM004 claim.

Final reviewed72 structure tests PASS2.163s; full609/605PASS/4skips59.163s,
base609/566PASS/43skips59.043s. Compile/wheel/installed v1+v2 FR/EN/pipcheck and
strict legacy L9-family consumer replay PASS. Sequential10k append.808516/
verify.357734/rebuild.808743s,cold16.997/hotp9516.524ms, unchanged budgets.
Shared host, no50k/100k qualification. PM_004M_local.json binds final sources.

No remote publication until dependency-qualified K main; retain L consumer
qualification order and then pin its exact main for integration. New24+3, review
and fresh main required. Initial green runs remain historical.


## Inherit finite-JSON correction and refresh local qualification

Owned M-only commit rebased from old K d278c89 onto corrected candidate f1a58d7.
Three expected conflicts resolved by retaining both append-only histories, both
installed regression fixtures (finite JSON plus v2 FR/EN) and provider profile8.
Inherited PHP parentheses/Windows locking and new JSON7a semantics are preserved.
Development parent only; K fresh-main qualification and L qualified integration
pin remain publication gates. No external work overwritten.

New73 structure tests PASS2.191s; full610/606PASS/4skips58.728s, dependency-free
610/566PASS/44skips56.872s. Compilation, rebuilt/installed wheel, both installed
v1/v2 FR/EN and finite-JSON checks, pipcheck, strict L9-family consumer PASS.
N actual feature-map development smoke passes with optional parsers and explicit
base coverage, but is not a qualified final consumer. Historical609 results stay.

First rebased10k measurement overlapped N quality/package startup; retained as a
shared-host diagnostic only. Sequential replay after checks: append.834917/
verify.367747/rebuild.806478s,cold17.561/hotp9518.453ms, unchanged budgets. No
controlled comparison or50k/100k qualification. PM_004M_rebased_local.json binds
current sources; initial local manifest remains historical. Core#109 initial
CI cancellation22success/2cancelled and final PR24+3 evidence archived here.


## Final combined M/O local candidate, 2026-09-20

Review adds the TypeScript function_signature barrier after two failing subcases.
Final80 structural tests PASS4.098s;617/613PASS/4skips107.631s. Initial base log
is incomplete despite exit0 and is not accepted as full evidence; unchanged
replay with structured result617/566PASS/51skips105.191s. Wheel/compile/pipcheck,
installed v1/v2 FR-EN/finite-JSON/literal-loader and qualified L9-family consumer
PASS. Sequential10k append1.647872/verify.705594/rebuild1.240121s,cold25.599/
hotp9532.170ms, unchanged budgets. Shared host; no controlled comparison, no
50k/100k or worst-case qualification. Earlier610/616 results remain historical.

PM_004MO_reviewed_local.json binds final code/tests/fixtures/workflows and raw
log hashes. Archived failure-log copies normalize trailing whitespace only.
Core parentc8a64fb, integration now qualified IdleProof6109ffec. Publish M/O
together, exact24+3/review/freshmain required. N development actual optional/base
passes, but its local Python p95155.5ms fails150ms; L baseline on same host also
fails220.5ms. No causal conclusion or passing N performance claim; all original
limits remain. This does not invalidate Core's separate passing10k measurement.

M/O supplies bounded source references, not complete canonical code surfaces,
runtime execution, package ownership, longitudinal identity or Alpha readiness.
PM004 and full registry scopes remain open. MACHINE only; HUMAN NOT RUN.
