# DiffWitness — PM-004g handoff

Read canonical Core issue#74, CONTRIBUTING and PM_004G.md before continuing.
Global NOT ALPHA READY; HUMAN NOT RUN. Prior qualified units remain closed.

This candidate extends the existing syntax/provider/transport/Git index with
Go/Rust. Required dependencies stay empty. Optional runtime0.25.2, JS0.25.0,
TS0.23.2, Go0.25.0 and Rust0.24.2 pins are deliberate. No progress callback:
its native crash reproduction remains in PM004f evidence. Parsing retains the
native250ms timeout and source/node bounds; no budget was raised.

Base is Core#105 main6b374e4a4584ac0331c637d6d1842f6b2a910b27,
tree b3b796190601b85f0d0bed9ad9b40e88e1f864b0. Candidate/main SHAs and remote
gates belong to#74 and later closure reports; do not infer qualification from
this pre-publication document. Required matrix24 plus proof/continuity/integrated,
review, identical merge tree and fresh-main push qualification remain mandatory.
Integrated IdleProof pin stays qualified30a2c5363d49b86d68574214b47c112c6bb75cee.

Final local569 tests/565PASS/four Windows skips,56.257s; compile, wheel,
actual installed multilingual extraction and pip check pass. Actual optional
tests are mandatory in threeOS jobs; dependency-free skips cannot qualify them.
Retain all failures: initial native-provider assertions, Rust comment-path
contamination, Go receiver-comment omission. The final latter fix selects the
actual parameter node; its regression includes a generic pointer receiver.
Local10k and Python budgets passed before that last source correction; exact
remote gates qualify the published candidate. No final50k/100k claim.

Native imports stay unresolved textual targets. Rust named implementation owners
retain lexical scope/trait names; unsupported complex receiver types are omitted,
not arbitrarily bound. Macro expansion, build flags, package/type/runtime
resolution and complete semantic coverage are not implemented. Profile3 forces
projection refresh and JS imports still cannot resolve to native-language files.

Next: finish IdleProof task-context adoption PM004h (isolated worktree), then
remaining feature-model consumer and language families, all-branch bootstrap
and longitudinal identity. Continue all full PM scopes/dependencies from#74.
Maintain reports/passations per unit, no early tag/release, no transferred HUMAN
PASS and no deletion of scope to announce Alpha. Check dirty work and fetch
before mutation. No subagents used; agent-operated paths are MACHINE.

PR review update:4051720040 reproduces two missing parenthesized receiver methods.
Correction unwraps a single non-comment type child, retains tuple rejection,
and bumps profile4. Read PM_004G_reviewed_local.json for corrected final source
and full qualification; earlier569-test/profile3 results remain historical.

Final reviewed/profile4 local570 tests:566PASS/four Windows skips,57.350s;
compile/wheel/installed multilingual producer PASS. Initial PR test35410721476
24/24, proof35410721494, integrated35410721450, continuity35410721481 all
PASS attempt1, but superseded by review correction; no merge of initial507f0ee.
New corrected exact-head and fresh-main gates are required.
