# PM-012n2 — Context latency without weaker freshness

Acceptance before implementation, 2026-09-18. The citation candidate's 10k local
sample fails the unchanged 300ms budget (p95 393.698ms). An interleaved comparison
on one identical fixture also fails with the preceding main implementation:
baseline samples 304.527–351.921ms, citations 306.453–321.478ms. No individual
SQL statement exceeds 2ms in the diagnostic run. Seven sequential Git processes
dominate this small local query; citations do not explain the shared overrun.

Reduce unnecessary process launches with unchanged public repository identity:
walk only the actual HEAD lineage directly; use the existing strict unborn check
only when that traversal fails. Other refs and temporary analytical objects must
not affect the root identity. Traversal failures on a committed repository must
remain errors. No fingerprint, HEAD, evidence or journal cache is added.

Context's required test command belongs to project configuration. Loading an
optional private engine profile contributes no context fields. Read project
configuration directly for that advisory packet, preserving validation of project
configuration and leaving all Proof/native assurance configuration checks intact.

Failure-first process budget and private-profile independence; committed,
unborn/first-commit, damaged Git, disconnected roots, native required-assurance,
full continuity and same 10k budgets. Record original failures and comparative
samples. Larger-history budgets and intermittent hosted Windows timing remain
separate open requirements. MACHINE evidence is not HUMAN PASS.

Local result: warm Git launches 7 -> 5, five process/config/lineage cases PASS.
The expanded HEAD-filename fixture initially needed the same explicit separator
in its own expected-value command; product behavior did not fail that case.
10k unchanged budget PASS after correction: cold 251.213ms, median 231.316ms,
p95 240.243ms; append 4.387818s, full verify 0.832455s, rebuild 2.118654s.
Initial failure and interleaved comparison are archived in evidence/.

Root traversal semantics are documented in [git-rev-list](https://git-scm.com/docs/git-rev-list).
