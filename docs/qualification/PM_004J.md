# PM-004j — shared Ruby/PHP syntax providers

Acceptance before implementation,2026-09-19. Development parent Core#107
c93a4cce065fb3514a6013a5e62059cc12b0ac84; publication waits for its exact-main
qualification. Global NOT ALPHA READY; MACHINE only; HUMAN NOT RUN.

Reuse the same optional syntax tree boundary, source-byte transport, immutable
Git-tree index and provider profile. Ruby/PHP named declarations and lexical
namespace/type/method scope, static literal require/include/use paths and inferred
name-only calls. No runtime dispatch, package resolution, source execution or
metaprogramming; unresolved dynamic receivers/generated symbols stay omitted.
Do not turn an import syntax observation into third-party ownership or Proof.

Before implementation, use actual pinned Ruby0.23.1/PHP0.24.1 regressions for
comments/strings/heredocs, namespaces/scopes/method signatures, static versus
dynamic targets, malformed UTF-8/syntax, absent grammars, transport source binding
and immutable-tree indexing. Empty/unparsed ambiguous identity keys remain explicit.
Core required dependencies stay empty, native250ms/no callback and source/node
bounds unchanged. Exact optional version admission and profile invalidation apply.
Preserve24 CI jobs and three companions, actual grammar behavior on3OS, review and
fresh main qualification. Remaining SQL/config, adapters and global scope stay open.

Primary manifests Ruby master0.23.1 and PHP tagv0.24.1 identify package pins.
PHP master advertises0.24.2, which is not available from the configured package
index: initial install fails and is retained. Released0.24.1 installs and matches
its tag. SQL0.3.11 is discovered separately, not implemented/qualified by this unit.
Grammar load succeeds locally with retained runtime0.25.2; this is discovery only.

## Local implementation and review

Ruby0.23.1/PHP0.24.1 reuse the existing syntax/admission boundary. Provider profile6
invalidates old projections. Ruby modules/classes, instance and self singleton
methods retain lexical scope; absolute constant scopes are not prefixed by an
unrelated outer module. PHP semicolon/block/global namespaces, classes/interfaces/
traits/enums and method signatures are retained. Dynamic receivers/anonymous and
nested runtime-generated declarations remain omitted. Static quoted require/use/
include paths are syntax observations; no actual package loading is inferred.
Ruby require_relative references are explicitly normalized with ./ when needed;
no filesystem resolver is invoked. Escaped/interpolated/dynamic path expressions
remain unresolved. Literal heredoc declarations are absent; real interpolation
calls remain inferred.

Initial eight-method suite yields eight failed assertions before implementation.
The first implementation passes all eight. Local review then reproduces arbitrary
PHP same-name namespace call binding: calls lack lexical namespace in the current
contract. A ninth regression fails before disabling that unsafe local binding;
raw declarations/name-only calls remain, and the real immutable-tree graph must
contain no arbitrarily selected calls-name edge.55 structural tests then PASS.
Final full/source/wheel/installed/base/platform/budget gates remain pending.

Ruby class reopening or duplicate name/kind keys still yield empty unparsed
coverage. This is an explicit limitation, not runtime correctness validation.
Constants, dynamic singleton receivers, metaprogramming, package origin and
runtime dispatch are not claimed. PHP unqualified calls are not linked to a
component-local symbol until a scoped contract can resolve them without choosing
one arbitrary namespace. SQL/config and all consumer adapters remain next work.

Final local588/584PASS/four Windows skips(56.951s); dependency-free
588/555PASS/33 explicit skips(58.054s). Compile, wheel, real installed multilingual
FR/EN extraction and pip check PASS. Windows amd64/macOS arm64 wheels downloaded;
actual3OS execution still required. Sequential post-suite10k PASS: append.841207/
verify.361290/rebuild.826792s,cold17.837/hotp9516.245ms. Budgets unchanged,
shared host; no50k/100k qualification. Parent#107 fresh main pending before publication.

PM004i Core#107 CLOSED exact-main MACHINE. Candidatec93a4cce065fb3514a6013a5e62059cc12b0ac84,
maina47c6d5b657aae993e6ab58eb66a4473386431f8, identical tree2283e4bb7c9c2a05fe5de77324f0c7103acd3e5d.
PR test35413970907 24/24 plus proof35413970861/continuity35413970860/integrated35413970862
PASS attempt1. Fresh main push test35414513294 24/24 plus proof35414513255/
continuity35414513215/integrated35414513263 PASS attempt1. Main clean/ref checked.
Bot +1, no inline findings. Local Kotlin kind failure and all earlier evidence
retained. Live logs for three running Windows jobs returned404 BlobNotFound;
final jobs and diagnostic artifact metadata retrieved. No rerun or budget change.
Report/manifeste/passation PM004i2026-09-19 produced; global NOT ALPHA READY.

PM004j owned WIP rebased without source changes from PRc93a4cce onto qualified
maina47c6d5b. Publication dependency gate met. Current integration keeps qualified
IdleProof042c94c7883cd47ca7cc6962398ec4e223039489. Local source hashes still match
588 tests, wheel/installed protocol and10k results. Exact PR24+3/review/main still
required; no remote qualification is implied by local PASS.

## PR review4051931757 — parenthesized PHP paths

Actual regression FAILs before correction: require('client.php'), commented nested
include_once and triple-parenthesized require_once yield zero rather than three
static targets. Unwrap only one expression through each parenthesized node; skip
syntax comments, retain dynamic/concatenation omission. Installed multilingual
producer now asserts the parenthesized import. Profile6a invalidates initial6.

Final corrected589/585PASS/four skips56.434s;56 structural tests, compile/wheel/
installed protocol/pip check PASS. Fresh sequential10k append.860561/verify.381102/
rebuild.795546s,cold22.732/hotp9522.175ms PASS unchanged budgets. Initial base588
suite remains historical; corrected remote base/full suites must qualify the new
head. Initial#108 CI state preserved separately and does not qualify this change.
New exact24+3/review and fresh main required. All evidence remains MACHINE.

## Blocking Windows concurrency finding — PM-012d within #108

Corrected PHP head1a239e6e28e6ee8f8f8690910ff1a23f3c6b5d8b fails test35415602574:
23/24 jobs PASS; Windows3.13 job105823576018 fails the existing real concurrent
append-chain/dedupe test with PermissionError errno13 creating events.lock.
589 tests/one error/31 skips419.994s on that runner. Companion proof35415602572,
continuity35415602556 and integrated35415602586 PASS. Initial pre-PHP-fix test
35415152663 is CANCELLED22success/2cancelled, not PASS; final state archived.

Acceptance before correction: retain exclusive O_CREAT|O_EXCL acquisition; on
Windows only, retry a denied creation within the original10s deadline/50ms poll.
Do not infer ownership, unlink, alter permissions or enter the journal critical
section from denial. Persistent denial must fail closed with original cause;
non-Windows denial remains immediate. No journal-byte/integrity/budget relaxation.
Windows can deny creation while prior deletion is pending, per Microsoft
[CreateFileW](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilew)
and [DeleteFileW](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-deletefilew).
This is a compatible causal explanation, not proof of the runner's exact kernel
state: the captured exception exposes errno13, no deletion-state trace.

Four new boundary regressions initially yield three errors/one PASS. After the
minimal acquisition-only correction,160 continuity tests PASS12.939s, including
transient recovery, persistent denial, foreign-owner exclusion and real concurrent
append/dedupe. Windows native CI still required. Stale-lock policy and unrelated
historical Windows intermittence are not declared resolved. No blind CI rerun.
Full corrected source/installed/budget/PR/main qualification remains required.

Lock-corrected final local593/589PASS/four skips57.247s;160 continuity regressions
PASS. Exact wheel and installed FR/EN protocol/pip check PASS after documented
invocation corrections. Sequential10k append.820174/verify.374459/rebuild.896854s,
cold18.210/hotp9516.199ms PASS, original budgets, shared host, no50k/100k claim.
PM_004J_lock_local.json supersedes reviewed_local.json for final source hashes.
Fresh corrected24+3, review and exact-main gates remain required.
