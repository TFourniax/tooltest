# PM-012n — Native prompt repository resolution

Acceptance before implementation. Base PM-012a #96 head
efc4c8d99025d3f407ba05455b8a8188b733a70d. Registry #74 is canonical.

Fresh-main tests after #94 and #95 independently exceeded the unchanged 10s
prompt-hook limit on Windows. First failures are preserved; no timeout relaxation.
Local native prompt profiling: 1319.4ms total, 28 processes / 1135.5ms subprocess
time, including 13 root resolutions and six identical common-directory reads.

Within one synchronous advisory prompt only, reuse Git-resolved repository and
common-directory paths. Use an exception-safe invocation scope with no persistent
cache, no cached HEAD/tree, journal bytes, digest, validation or Proof decision.
Different repositories and linked worktrees retain distinct roots and correct
shared journal paths. Every following invocation resolves again; ordinary public
calls outside the scope retain their original freshness behavior.

Regression-first actual Git process-count budget, sequential invocation isolation,
exception cleanup, linked worktrees, fresh task/context contents and corruption
rejection. Existing native handoff, continuity and full remote gates are required,
then authorized merge and fresh exact-main qualification. Repeated CI timeouts
remain recorded until the candidate qualifies; HUMAN NOT RUN.

## Additional reproduced context determinism defect (PM-008a0)

The broader 109-test run exposed an intermittent indexed/exhaustive difference
in `relevanceReason` for equal graph paths. Reversing rows/edges reproduces two
failures with a fixed Python hash seed, including exact-ID queries. It is not
a reason to relax equivalence assertions. Canonically order relation edges and
graph frontiers before existing scoring so ties choose the same explanatory
reference across query plans and process hash seeds. Preserve complete two-hop
candidate scope, epistemic labels and existing score/depth formulas. This is a
bounded determinism correction, not full Context Compiler v2 acceptance.

## Local qualification

- Native resolution/corruption: 5/5; existing native plugin: 5/5, original limits.
- Graph equivalence at hash seeds 0/1/7/29 and full continuity: 109/109.
- Coordinated exact wheel + sidecar smoke PASS with IdleProof #6 qualified tree.
- Strict UTF-8 harness output regression PASS for both acceptance scripts.
- Initial cp1252 harness failure, graph mismatch and all earlier timeout failures
  remain journaled. Compilation/diff review PASS. Remote/fresh-main required.
