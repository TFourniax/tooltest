# HT-016 — verification readiness and current-code Proof coverage

Baseline independently resolved before branch creation: `53887986e5fd346a0b692560ef9c53f06436603c`, tree `4e5da6eb4068f403cda51903c4f0b541dd63ec9d`. Exact-main runs 34409626309 / 34409626292 / 34409626297 / 34409626361 all SUCCESS. Dedicated branch: `fix/doctor-stale-proof-truthfulness-20260910`.

## Reproduction before implementation

`ht016-before.json` records actual English CLI outputs, exit codes and canonical readiness projections from this baseline. Fixture: tracked Python addition function initially subtracts; unittest expects 2+3=5; explicit executable Python test command; strict Guard changes subtraction to addition and produces an accepted causal Proof. Then change addition to multiplication, commit that unverified drift, and run another strict Guard attempt changing multiplication to division (rejected). No fabricated envelope or mocked acceptance.

| State | Command configured/executable | Proof status | Current tree verified |
| --- | --- | --- | --- |
| A: no Proof, clean baseline | true/true | unknown | false |
| B: accepted fix | true/true | accepted/fresh | true |
| C: actionable drift | true/true | stale | false |
| E: commit drift without new Proof, clean tree | true/true | stale | false |
| D: failed subsequent Guard attempt | true/true | stale | false |

D naturally retains the earlier accepted historical envelope; it does not publish an accepted replacement. No requirement to fabricate an inconclusive envelope. The existing canonical readers agree across doctor/status/setup in every state. `checksRun=false` remains true of these preflights. Doctor exit 0 denotes its existing scoped preflight, not current-code validation; this exit contract is unchanged.

The historical broad claim of a missing stale flag is falsified: doctor already prints `Current Proof: stale · current tree verified=False`. The narrower human ambiguity is reproduced: it then prints `Evidence: ready`, or `Verification ready`, and directs the user to setup without explaining historical coverage or asking to verify the current change. Status Technical repeats the evidence/launcher ambiguity. Status Guided's clean-tree summary says “the project has no uncovered change” even in E, while its canonical Proof is stale. These are renderer defects; no canonical-state defect was found.

## Correction and boundaries

One pure `proof_human_lines` helper presents the existing readiness.currentProof in doctor and both status views. It shows the canonical freshness/verification flag, explicit current coverage only when currentTreeVerified is true, historical non-coverage for stale Proof, and an action to verify the current change when not covered. It never calculates identity or acceptance. Doctor explicitly distinguishes command configured/executable and says it does not run checks. Technical launcher labels no longer call launchability “Evidence ready”. Clean status wording no longer implies coverage.

Setup/readiness was inspected: setup status describes integration and verification preflight; it does not claim current Proof coverage. No setup modification is needed. No changes to readiness builders, status model/actions, JSON schemas/keys/enums/reason codes, exits, certificate/authority/identity/native/runtime/provider/Protect/Continuity/Portal logic. French uses the existing optional presentation selector. LANG-001 remains closed; no French polish.

Targeted regression `tests/test_doctor_proof_truthfulness.py` executes A/B/C/E/D with real strict Guard evidence. At every state it checks both public commands, both views and both languages; exact JSON/exit-code invariance; doctor/status Proof equality; and unchanged source/config/HEAD/index/hook/envelope hashes, with no Claude configuration created. The same test runs against the installed wheel on all three CI OSes. It also fails on the uncorrected presentation (missing explicit no-coverage/action lines).

Local qualification: 351 tests PASS (39.438s); ProofBench 4/4 PASS; ContinuityBench 10k PASS (hot p95 149.028ms); Integrated Product Smoke PASS with companion `49c4daa04088e3ba270e9e97810dd40e48bad29b`. Authoritative candidate/run/package evidence is recorded in the PR after exact-head CI completes. No human PASS is claimed by automated consumer journeys.

## Targeted HUMAN procedure (after exact candidate is machine-green)

Use a fresh disposable project and the exact installed candidate recorded in the PR; verify installer provenance. Do not reuse prior qualification fixtures or run provider-real Protect tasks.

1. Create a tracked Python project with `calculator.py` defining `add(a,b)` as `a-b` and a unittest asserting `add(2,3)==5`. Configure `[diffwitness] test` with your explicit Python executable plus `-m unittest -q`, and `stability_runs=1`. Commit this broken baseline.
2. Through `dw guard --policy strict --strategy auto --stability-runs 1 -- <your Python> -c <script changing a-b to a+b>`, produce an accepted Proof. Confirm doctor/status JSON agree on currentTreeVerified=true and matching candidate/current trees. Both human views must describe current exact coverage.
3. Manually change the tracked function to `a*b`, without producing another Proof. Save hashes of source, HEAD, index and any existing hook files. Run doctor and status in Guided and Technical, plus their JSON forms.
4. Both JSON projections must report stale/currentTreeVerified=false; launcher readiness may remain true. Both human views must explicitly state that the latest Proof is historical and does not cover the current code, with a next action to verify it. Doctor must distinguish configured/executable readiness and say it does not run checks.
5. Compare source/HEAD/index/hook hashes after those read-only commands: unchanged. No new provider configuration should appear. Record candidate SHA, outputs, exit codes, JSON comparisons and hashes.

No provider session, destructive action or Protect requalification is needed. Freeze for owner review and this targeted HUMAN qualification. Do not merge or start another finding.
