# DiffWitness — PM-012b continuation handoff

The single current registry is [Core #74](https://github.com/TFourniax/tooltest/issues/74).
This document records the bounded implementation; its later PR/main qualification
and exact SHAs must be read from that registry. The product remains **NOT ALPHA READY**.
Do not close #74, drop full PM scopes, publish a release/tag, or transfer historical
HUMAN results to a new candidate.

## Unit and evidence

Base Core is `4f8f9b12f19fff7860ec7bfed4c885971a0e0cb5`, tree
`e8a66ec457630db28072a262cc6895018164bca7`. IdleProof remains unchanged at
`97fcd0286026b99bdc309454d75a43e1f81a2f33`; the existing integration pin stays intact.
Only Core is modified. Acceptance and implementing-agent review: `PM_012B.md`.
Operations/failures: `JOURNAL_2026_09_18.md`. Local evidence: `evidence/PM_012B*`.

Appending now validates the old history once and continues the same request-local
chain/dedupe/task/lifecycle validators through the new batch. Materialization still
strictly parses and validates the full journal; a compatible state whose sequence,
ID/hash rows form that validated prefix projects only new events. Existing projectors
and the lexical index remain the single projection implementation.

Derived-state updates, structure refresh writes and explicit reconstruction serialize
on a separate local state lock. Snapshot reading occurs after lock acquisition.
Rows and exact validated byte digest/count/head commit together. Failure rolls back;
incompatible/divergent/truncated/damaged state reconstructs from the same snapshot.
Busy or I/O errors are not hidden by replacement. Existing timeouts and budgets stay.

## Qualifications and limits to preserve

All evidence produced by this agent is MACHINE. The local full suite runs 533 tests:
529 pass, four Windows-only skips. The earlier run failed six installed-product
checks because the installed scripts were absent from PATH; corrected invocation
and complete rerun are recorded. Initial operation-count/snapshot regressions fail
before implementation. The old second-read corruption error was fail-closed, not a
security bypass. An old race fixture was moved to the new lock boundary so that its
interleaved writer actually executes; its outcome assertions remain intact.

The benchmark records 10k/50k/100k under unchanged existing 10k limits. Failures at
larger scales are failures, not newly qualified product budgets. A separate diagnostic
measures real append-one and materialization after synthetic fixture construction;
fixture construction is excluded and explicitly is not an append benchmark. It also
checks all logical tables against reconstruction and counts projected events.

Full journal verification and the materialized-prefix comparison remain O(history).
There is no persisted incremental validation checkpoint, no mtime/size trust, and
no full PM-012 completion claim. No monorepo or universal latency claim follows.
Derived caches are not cryptographically authenticated. The new local lock does not
coordinate older binaries, independent emitters or arbitrary SQLite writers.
Historical Windows intermittence and 50k/100k failures remain in prior evidence.

## Resume and next scope

Freshly fetch #74 and exact refs/PRs before mutation. Preserve dirty work. Qualified
merges are authorized only after exact candidate qualification and review, followed
by fresh main qualification. The user requested a clean unit closure and this handoff.
No original Windows files were accessible from this isolated Linux continuation.

Use measurements to bound the next PM-012 unit: the remaining strict full-history
parse/hash/profile/reference work dominates updates. Any incremental validator must
retain byte-level tamper detection and ordered task/lifecycle reference correctness,
including corruption, divergence, same-size edits, concurrency and cache reconstruction.
Do not weaken limits or present a prefix-only check as whole-history integrity.

Then follow the dependency order in #74: providers/languages and lineage; drift and
revalidation; complete cited context and FR/EN evaluation; why/history and impact;
multi-emitter protocol; longitudinal Portal store/UI/cited answers; explicit user
understanding memory; PM-018 and exact-candidate human journeys; distribution #80.
The source-reference consumer/Portal gaps and Git bootstrap limits from the preceding
handoff are unchanged. Supabase/Portal work requires its applicable skills.
