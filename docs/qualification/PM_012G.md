# PM012g — detach JSON event ownership without generic-copy overhead

Design before implementation. Parent candidate PM007 `e828d28181a3064d83a413af2671efcb1e5e5b69`,
tree `91c875d38326cfc68eb2edc9b5046635c3330d37`; its final PR/main qualification
is still pending. No dependent merge before that qualification.

Qualification base advanced after PR115 completion to main
`8c0481f5107c500b125cdfa368670c60c2ec8e9e`, tree
`c82908377a5f74a594c5d456debb42ad7e308fd6`, fresh24+3 SUCCESS attempt1.
The prototype was replayed onto that main with a local checkpoint preserving its
original version. Integrated consumer CI now uses qualified IdleProof a37cb4f,
matching the existing actual-hook consumer; no old sidecar qualification is implied.

One instrumented 2,000-event synthetic run shows generic `copy.deepcopy` spending
0.278 seconds cumulatively out of 0.560 seconds in append (1.213 seconds for the
whole benchmark). These profiler timings are diagnostic, not release latency.
Append detaches both caller-owned event data and public results to protect the
private, byte-bound validator checkpoint. Both ownership boundaries must remain.

Try a specialized copy for exact built-in JSON dictionaries, lists and immutable
primitive values. Preserve aliases and cycles with a fresh memo per operation;
delegate tuples, subclasses and other values to the existing deepcopy using that
same memo. Do not normalize types, reuse caller-owned mutable containers, alter
event encodings/IDs, add a parser, or change any journal-byte admission/checkpoint
rule. If the measured cost does not improve, do not ship this optimization.

Acceptance: copy-equivalence and isolation including aliases, cycles, subclasses,
tuples and negative zero; existing input/result mutation, corruption, concurrent
append, lifecycle and checkpoint regressions; full base/optional suites, compile,
installed exact wheel and integration. One parent and one candidate run per size
(10k, 50k, 100k) with the existing gates unchanged, plus explicit retention of
every failure. Existing 10k thresholds at larger sizes remain discovery rather
than a newly agreed SLA. This does not solve persisted incremental validation,
monorepo or Windows qualification, and does not close full PM012.

## Exploratory results

Parent 50k append5.736623s / verify2.594193s / rebuild5.703481s;
100k append12.369307s / verify5.215382s / rebuild11.451622s.
Candidate 50k append4.909132s / verify2.581847s / rebuild5.490482s;
100k append11.404256s / verify5.181006s / rebuild11.521608s.
Larger-history gates remain FAILED. Sequential samples do not establish a
universal speedup; observed append reduction is 14.4% at50k and7.8% at100k.
Candidate10k passed (append0.979343s,verify0.475193s,rebuild0.960187s); its first
seconds overlapped the short PM007 review regression run, so it is not a clean
paired latency comparison. No failure is relabeled as a pass. The original
12-pair copy microbenchmark preceded the atomic-type lookup refinement.

## Final local qualification on the qualified base

Base666 tests:614PASS/52skip in68.752s; fresh optional666:662PASS/4skip in68.908s. Compilation and wheel build pass. All101Python files match source, wheel and actual installed optional consumer; wheel SHA256175ed30196fc1306e20b52a92b033435795442a60583ea47536913fbf5248e56. Actual installed FR/EN code-reference journey and exact-artifact integrated product smoke with IdleProof a37 pass.

Fresh final installed10k: append0.837949s, strict verification0.379842s, rebuild0.880623s, hot context p9517.803ms, all unchanged gates pass. A separate12-pair alternating copy-only diagnostic has median generic0.019032s versus candidate0.011187s (ratio0.588). This isolates copy cost; it does not establish end-to-end or universal latency improvement. Earlier50k/100k failures and the overlapped exploratory10k sample remain unchanged.

All raw logs and measurements are retained with hashes in PM_012G_local.json. The initial build invocation lacked the build module; the existing setuptools backend built the wheel. An installed journey invocation lacked dw on PATH; passing the exact executable resolved that invocation failure. No failure was replaced. Fresh PRCI, review and main qualification remain necessary.
