# PM-004i — Java, Kotlin and C# shared syntax providers

Acceptance before implementation, 2026-09-19. Development parent is corrected
Core#106 head2a3b01e; its exact-main qualification is required before publication.
No earlier unit is reopened. Global NOT ALPHA READY, MACHINE only, HUMAN NOT RUN.

Extend the same optional Tree-sitter boundary, registry, source-byte transport and
immutable-tree materializer to Java/Kotlin/C# named declarations, member methods,
and source-level import/using paths. Preserve lexical package/namespace/type scope
without asserting runtime/package/type resolution. Use actual grammar nodes;
comments, regular/raw/interpolated strings must not invent declarations or paths.
Anonymous/complex/generated symbols, annotation processing, macros/preprocessors,
build configuration, Kotlin extension dispatch and semantic overload resolution
are not claimed. Ambiguous name/kind identities must remain explicitly unparsed.

Keep dependency-free Python/unknown fallback, required dependencies empty,
exact optional version admission, native250ms timeout and node/source bounds.
Do not invoke the crashing native progress callback, javac/Gradle/dotnet, runtime
installation, network access or project code. All observed/inferred authority,
source hash, immutable Git-tree capture and transaction validation gates remain.
No native-language imports may become JS/TS local resolutions.

Before implementation, add actual-parser regressions for scopes, method/type
declarations, import aliases/wildcards, Unicode and decoys, invalid syntax/UTF-8,
missing grammars, ordered transport/source binding and immutable-tree indexing.
Check package provenance/versions and binary wheel availability on the three
supported OS. Keep24 Core jobs and all companion workflows, actual optional
grammar jobs without skips, exact-source review and fresh-main replay. Remaining
language families and full feature-map/consumer adoption stay open. No tag/release.

## Implementation and review

Source-bound managed_declarations consumes only the syntax tree admitted by the
existing extract_syntax. No second parser, storage or transport. Profile5 refreshes
derived projections; optional dependencies Java0.23.5/Kotlin1.1.0/C#0.23.5 match
the primary grammar package manifests. Windows amd64 and macOS arm64 wheels were
downloaded, and Linux wheels installed. Downloads establish availability only;
actual platform behavior still requires the threeOS mandatory grammar jobs.
An initial pip index query timed out twice then succeeded. This is an environment
diagnostic, not a parser or product PASS/FAIL; preserve it in evidence.

First7 tests yield8 assertion failures before implementation, including three
missing-grammar admission subtests. Initial actual45 structural tests PASS after
review found Kotlin class_declaration also represents interfaces/enums/annotations
and bodyless members. A new kind/signature regression FAILs before keyword-node
classification; no source-text guess is used. Comment-path imports and actual
identifier-only calls are covered. A further raw-string/interpolation test passes:
literal declaration decoys stay absent while real interpolation calls remain.

The compact Kotlin class fixture produces a real MISSING _class_member_semi in
the pinned grammar. It remains an explicit empty unparsed regression, not a text
repair or hidden parser success. Duplicate overload name/kind identities likewise
stay unparsed. These are limitations of this bounded unit, not full PM004 closure.
See STRUCTURE_EXTRACTION.md for all omitted forms and semantic limits.

The original development parent2a3b01e was preserved in a local WIP commit, then
rebased without content conflicts onto merged#106 main0c342bc2fb12edb2d58da39958e3c0667a22a1db.
That parent's fresh-main qualification remains a publication prerequisite. The
initial local full578/574PASS/four platform skips(57.149s) and installed multilingual
producer pass; final full/base results, actual source hashes and log digests are
recorded in PM_004I_local.json when complete. No pre-review/future result is implied.

Final local579 tests/575PASS/four Windows skips(56.217s); dependency-free
579/554PASS/25 explicit skips(56.545s). Actual optional parsers qualify behavior,
not skipped cases. Compile/wheel/installed multilingual FR/EN protocol/pip check
PASS. Sequential post-suite10k diagnostic PASS: append.875599/verify.358022/
rebuild.811659s,cold15.791/hotp9515.244ms. Shared host, no50k/100k claim.
Exact source/log hashes in PM_004I_local.json; remote gates still pending.

## Coordinated dependency qualification

PM004h IdleProof#11 CLOSED exact-main MACHINE. Corrected candidate0e960aa8fc25876e6cd096fe4ab126008e5dd5e4,
main042c94c7883cd47ca7cc6962398ec4e223039489, treee937a3a2bb19db3dc3f2d5c536495b18086ccedb.
PR35413554252 and fresh main push35413691822 each16/16 PASS attempt1.
Initial green CI35412716165 was superseded after review4051821830 reproduced
local/platform imports falsely labelled third-party integration. Raw imports stay
canonical; task dependency selection corrected and actual3OS regression passes.
Full package-origin resolution remains open. Failure logs and reports preserved.

Integration now pins this qualified IdleProof main. Core parent0c342bc2 is already
qualified24+3 on its exact main. Both publication dependency gates are met. Core
production source is unchanged since the final579 tests/wheel/10k qualification;
the updated integrated journey and all exact-head remote gates remain required.
Global NOT ALPHA READY; HUMAN NOT RUN; no tag/release.

Local installed exact-artifact integrated journey PASS with qualified IdleProof042c94c: setup/trust lifecycle, native hooks, dirty baseline, Understand/Prove/Owe/Continuity and uninstall evidence preservation. Machine-operated only.
