# PM-012e — repository-free source extraction startup

Acceptance before implementation,2026-09-20. Repair parent merged PM012d main
e6e0419b61c35c0e2108b0ef4e3ab4ca3c97b23d remains NOT QUALIFIED: fresh
main test35518471617/job106098317742 fails real JSON/Core hookp95164.9ms
against150ms. Same job actual feature smoke and Python78.4ms/TS107.5ms pass;
PR112 had all24+3 success and review, but that does not qualify main. Preserve
the failure and do not automatically retry until green or relax any threshold.

Inspect remaining startup dependencies. The entrypoint, language module,
continuity CLI and source-size constants still import Git/repository helpers
while the bounded stdin extractor deliberately never resolves a repository.
continuity_cli also imports the complete Project Memory contract before it knows
which command is requested. Optional syntax imports importlib.metadata eagerly
even for Python-only requests. Initial JSON import profile retained; measured
on a shared host while other source tests ran, so its cumulative microsecond
values are diagnostic import attribution, not a latency comparison or proof of
the CI failure's root cause. Do not assume the failed runner was identical to PR.

Move only unused imports to the existing functions that need them. Keep the
existing parser, public entrypoint, saved/explicit language semantics, canonical
provider catalogue, exact pinned-grammar checks, strict JSON/source admission,
all language provider behavior and15s/60s history bounds. No source/results cache,
new worker/daemon, alternative parser, parallel hook implementation or relaxed
budget. Source extraction must remain independent of Git and Project Memory
contract initialization; Python extraction must not initialize optional package
metadata lookup. Actual grammar metadata is still checked on every relevant call.

Strengthen fresh-subprocess isolation before coding for both protocols and FR/EN,
including mixed Python/JSON transport and explicit malformed-input behavior.
Compare installed response bytes and alternating before/after timing samples on
the same quiet local host after other tests finish; real hooks remain the existing
unchanged source-pinned CI gate. Full/base,compile,wheel/installed,10k,24+3,
review and fresh exact-main required. If main later passes via this correction,
record it as a new qualified source; never relabel failed PM012d main attempt1.
PM005a publication and global/HUMAN/release remain blocked meanwhile.

## Initial repair and measurements

Original strengthened isolation:4subcasesFAIL on eager gitops. Adding Python-
only optional-metadata isolation produces6FAIL across8tests before correction.
After command-local Git/contract imports, local metadata import at version-check
use and deferred typo-suggestion difflib, all8transport tests PASS2.018s. The
initial before test subprocess selected source-equivalent parent D through its
editable environment; subsequent before/after use explicit absolute source path,
and full qualification uses dedicated optional/base editable environments.
No provider implementation, registry selection or admission is replaced.

Alternating20pairs per family, exact installed D and E wheels, source responses
byte-identical; no other task tests overlapped. Median(ms) Python95.022→76.157,
TS94.887→91.063,JSON94.945→91.599. p95Python140.374→96.409,
TS128.548→119.745,JSON114.027→122.399 (worse). Raw samples retained; this
is not evidence that the main CI failure's scheduling cause is known or fixed.
Fresh actual local hooks:Python144.6msPASS,TS183.8msFAIL,JSON191.2msFAIL
against150ms. Each family ran once and all failures are retained. Do not claim
all-family local performance or universal latency. The exact CI gate remains
required, with unchanged budgets; fullPM012/host variability stays open.

Initial634full tests produce one ERROR (4platform skips): the existing corrupt-
blob snapshot regression accesses structure_sources.git_bytes, whose eager alias
was removed. Preserve that existing callable via a late-bound wrapper to the
same Git reader; tree_sources still uses it, and the corruption test is unchanged.
Original full log and error excerpt retained. Seven snapshot and8transport tests
PASS after correction. Initial installed paired/hook samples precede this wrapper
compatibility correction, so final exact CI remains authoritative for the final
candidate. No source cache or integrity-test weakening; full/base replay follows.

## Final local source gates

Final634full tests:630PASS/4platform skips109.620s. Base634tests:
582PASS/52explicit skips114.364s. Compile,wheel,pip check,installed19fixture
syntax FR/EN/v1/v2,Node transport,captured all-branch history and integrated
exact-wheel/sidecar journey PASS. All96Python wheel and installed package files
are byte-equal source; wheelSHA256
2410eb0601920975c443ec6fe707b338b3fe82cf10ae443753dca3472e97ef0f.
The first installed journey was inadvertently started while the rebuild was
still running; only the final-suffixed logs replayed after completed installation
are used for final source qualification. No pass is inferred from that overlap.

Sequential10k PASS:append1.247043s,fullverify.519267s,rebuild1.196295s,
cold25.515ms,hotp9534.149ms. Local hook failures above remain unchanged.
PM_012E_local_manifest.json binds source and log hashes. Exact24+3 PR/review
and freshmain gates remain required; PM012d main stays failed permanently.
