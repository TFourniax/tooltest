# PM-004d shared extraction handoff

Read canonical Core #74 for live exact refs/qualification. Global NOT ALPHA READY.
The bounded `dw state extract --json` protocol exposes existing source-bound Python
AST extraction through stdin/stdout, outside Git. It validates complete requests,
never opens source labels or persists source, and returns explicit file-only
fallback for unknown languages. It does not qualify the full language set.

Contract: docs/STRUCTURE_EXTRACTION.md. Acceptance/review/failures: PM_004D.md.
The real Node consumer runs against an installed Core wheel and is integrated
into the existing exact-artifact workflow; FR/EN installed wheel/binary journeys
also cover the command. Final local 551 run / 547 PASS / four Windows-only skips,
compile/wheel/actual Node/FR-EN PASS. Initial missing-interface and wrong-worktree
installation failures remain in evidence, not erased by subsequent passes.

Next adapter is IdleProof PM-004e: preserve its source admission, validate producer
hashes/shapes/authority and use the same Python provider for task symbols/imports.
Do not turn cache/source metadata into Git/Proof authority. Continue actual language
providers and feature-map adoption before longitudinal identity and remaining #74
dependencies. Qualified merges need fresh main gates; HUMAN must be performed by
a real human on the exact final candidate. No release/tag or premature #74 closure.
