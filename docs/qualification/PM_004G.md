# PM-004g — shared Go and Rust syntax providers

Acceptance before implementation, 2026-09-19. Depends on PM-004f exact-main
qualification; current development parent is its PR head7e8894f. Extend the same
optional parser boundary, FileExtraction registry, transport and Git-tree index.
No second parser transport, store, event identity or truth authority.

Bounded scope: Go functions, receiver methods and named types; Rust functions,
structs/enums/traits/type aliases/modules and implementation methods; source-level
Go import literals and Rust use/extern-crate paths. Syntax facts are OBSERVED,
name-only calls INFERRED. Do not claim module/package/trait resolution, macro
expansion, build configuration, dependency availability or runtime dispatch.
Never execute go/cargo/project code or download packages at extraction runtime.

Reuse the pinned native parser timeout, node/source/fact limits, error/UTF-8
rejection and missing-package fallback from PM-004f. Preserve Python and JS/TS
facts/IDs. A new language suffix must not let JS/TS import resolution invent edges
to Go/Rust files. Provider profiles must refresh derived state when these grammars
become available/unavailable. All admissions precede writes to the prior index.

Regression-first evidence must use actual installed parsers, including grouped
imports/aliases, receivers/implementations, Unicode, string/comment/raw-string and
macro decoys, malformed syntax, missing optional packages and immutable-tree
indexing. Keep existing21 CI jobs plus3 actual optional-provider jobs, all three
companion workflows, exact-source review and fresh-main qualification. No skips
may qualify actual parser behavior. Full requested language/consumer scope and
global Alpha remain open. HUMAN NOT RUN; no tag/release.

## Implementation and review

The acceptance paragraph above records the initial parent. Development was
preserved in local WIP commits, rebased onto corrected #105 head32ec0d5 (two
owned conflicts resolved), then onto its merged main6b374e4. No unrelated work
was overwritten. Exact-main parent qualification is required before publication.

The shared registry selects Go/Rust grammars; structure_native.py walks their
admitted syntax trees. It reuses FileExtraction validation, transport and index
transactions. Profile3 refreshes earlier projections. Go/Rust imports retain
external textual targets; JS/TS relative resolution stays limited to JS/TS.

First6 regression methods yield6 assertion failures including subtests; the
cross-language import isolation method already passes. Actual structural tests
progress32 ->33 after owner-scope review ->35 after corrected parent ->36 after
comment-path regression. Local review reproduces comments entering Rust import
and receiver names; identifier-field assembly corrects that. Tuple receivers
are not assigned an arbitrary child type. A second review reproduces a comment
before a Go receiver hiding its method; selection now uses the actual parameter
node, with a generic-pointer receiver regression. Both before-fix logs remain.

The unreviewed native candidate passes569 tests/565 PASS/four platform skips
(58.118s); dependency-free569/553 PASS/16 skips(58.045s). Actual optional grammar
jobs, not these skips, qualify parser behavior. Installed wheel, pip check,
multilingual protocol and unchanged10k budgets pass. The last receiver correction
requires a fresh full run/wheel/consumer before the candidate is frozen; final
results and hashes are recorded in evidence/PM_004G_local.json.

Initial local10k diagnostic: append0.797002s, verify0.353772s, rebuild0.783750s,
cold15.481ms, hotp9522.212ms. IdleProof Python consumer p9584.8ms/max104.6ms,
Stop120.9ms/state0.3ms. Shared host, sequential benchmark after full tests, not
an isolated statistical comparison or final50k/100k qualification. No budget
or timeout raised. Historical long-scale and Windows intermittence limits stay.

Unsupported semantic resolution, macros/build flags and complex receiver types
remain explicitly outside this syntax unit; see STRUCTURE_EXTRACTION.md. No
full PM-004, consumer-unification, longitudinal identity or Alpha completion.

## PR review correction

Core#106 initial head507f0ee810fd257c1dc8b6094c571141dc1c59bc receives
automated review4051720040: parenthesized named Go/Rust receivers omit methods.
One new method produces two assertion failures with actual grammars before fix.
Unwrap only pointer/parenthesized type nodes with exactly one non-comment child;
complex tuple receivers remain unresolved/omitted. Profile4 invalidates earlier
projections, including initial PR profile3. All initial local/CI evidence remains
historical; corrected full/wheel/CI qualification required before any merge.

- First wrapper fix passes Go but the full570-test run still fails one Rust
  assertion(56.603s). Actual AST inspection shows Rust represents (T) with
  tuple_type, unlike Go parenthesized_type. Correct only a comma-free tuple_type
  with one non-comment child; (T,), (A,B) and () remain omitted. Nested parentheses
  and a comma inside a comment are covered. No arbitrary type binding or test
  deletion. Initial full failure excerpt and original log hash retained.

Final reviewed/profile4 local570 tests:566PASS/four Windows skips,57.350s;
compile/wheel/installed multilingual producer PASS. Initial PR test35410721476
24/24, proof35410721494, integrated35410721450, continuity35410721481 all
PASS attempt1, but superseded by review correction; no merge of initial507f0ee.
New corrected exact-head and fresh-main gates are required.
