# DiffWitness — PM-012c handoff

Read canonical [#74](https://github.com/TFourniax/tooltest/issues/74) for exact
candidate/main refs, workflows and artifacts. Baseline is Core d5e0bfe; other
three main refs are unchanged from PM-012b. Global status remains NOT ALPHA READY.

PM-012c adds bounded process-local append validation reuse only after comparing
every byte of the previously validated prefix. No validator is persisted; public
verification/reconstruction stay complete. Checkpoint ownership is taken before
speculation and returned only after durable success. Deep copies isolate caller
objects. Valid unterminated imported records receive a separator on append.

Acceptance, implementation review, failures and limits: PM_012C.md. Evidence:
evidence/PM_012C*. Local qualification includes twelve regressions, 155 Continuity
cases, the full suite and wheel. Initial baseline regression and runtime setup/
child-import failures remain recorded. Large-history budget failures are real:
50k verify/rebuild and 100k append/verify/rebuild still fail. The checkpoint adds
bounded retained objects; 128 MiB is the journal-byte cap, not total process RAM.

Safe persisted validation remains open, as do full PM-012 and every unqualified
product scope. Continue providers/languages and shared Core/IdleProof adapters,
then bootstrap/longitudinal identity, drift/revalidation, complete cited context
and FR/EN retrieval, why/history and impact, multi-writer, Portal storage/UI/cited
Q&A, explicit user memory, PM-018, exact-candidate human journeys, distribution #80.
Use existing substrates; no parallel truth system and no reduced Alpha scope.

Qualified merges are authorized, followed by fresh exact-main qualification.
No tag/release before final gates; HUMAN only means real human validation.
No sub-agent has been used. This Linux continuation preserves older checkouts.

PR #103 first matrix failed all four Windows Python jobs (35402939988). CRT text
translation changed written LF into CRLF, defeating cache reuse safely. The fixed
writer explicitly emits native endings with O_BINARY; a thirteenth regression
reproduces the original failure. Corrected local: 156 Continuity PASS, full
546 run / 542 PASS / four skips, compile/wheel/final 10k PASS. Initial failure
evidence is retained. Read #74 for the corrected exact PR/main qualification;
do not treat the initial candidate as qualified or the scale failures as solved.
