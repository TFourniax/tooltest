# DiffWitness — handoff after the 2026-09-18 work unit

This file is a dated navigation aid. The [single current registry is #74](https://github.com/TFourniax/tooltest/issues/74).
Its latest exact-commit qualification checkpoints take precedence over older
dated notes. **The global product is NOT ALPHA READY.** Do not close #74 or the
release gate #80 merely because this bounded work unit qualifies.

## User requirements that persist

- Continue toward the complete Alpha product vision, preserving all PM-001–018
  requirements. Complete concrete work, tests, analysis and a report.
- Qualified PRs may be merged; always verify fresh `main` after each merge.
- Keep the registry and repository journal current, including initial failures,
  reruns, exact commits/trees and artifact evidence.
- `HUMAN PASS` requires a human's real-condition test on the actual candidate.
  Scripts, CI and agent-operated journeys are MACHINE evidence. Historical human
  passes never transfer automatically to the new candidate.
- Close each validated unit cleanly and provide a contextual handoff for the next
  conversation/model. Do not call the entire product finished while gates remain.

## Completed or qualifying changes to read first

Core #93–99 add durable task memory, immutable lifecycle review, Git-bound Python
structure, indexed context, deterministic graph ties, Unicode native protocol
and provider-neutral extraction admission. Their exact-main proofs are recorded
in #74 and `evidence/`. IdleProof #5–9 add typed Core consumption, snapshot identity,
Unicode/task compatibility, required-assurance failure handling, bounded local
source admission, Windows lock contention handling and byte-bound context caching.
IdleProof #9 main is `97fcd0286026b99bdc309454d75a43e1f81a2f33`, tree
`55482e2dd00dc27ce8a058c46cd4b6c16f3eaaa4`; PR/main test runs 35343640417 and
35343925125 each PASS 16/16. This branch's coordinated CI pins that exact main.

Core #100 adds opt-in bounded first-parent history bootstrap with immutable page
continuation. Main is `660ffc3e9795846f32100d4ac84a76887f6d3b75`, tree
`0b880d4dbf18d898c454b5d5083af3b4609fa05e`. PR test 35342745464 passes 21/21
after a frozen Windows timeout and same-commit rerun; three companion workflows
pass. Fresh-main test 35345107320 first fails two native Windows 10s cases;
attempt 2 then qualifies 21/21, with all three companions PASS. Failure evidence
remains archived; this is no claim that every hosted timeout is eliminated.

The current unit adds exact assertion source ID/hash to selected memory, one
SQLite read snapshot, bounded source lookups, fewer context Git processes and
atomic derived-schema creation. Read PM_008A1.md, PM_012N2.md, PM_012S.md and
JOURNAL_2026_09_18.md. Final exact PR/main qualification must be checked in #74;
this document cannot contain its own later commit identity.

## Known limits and next work

The 10k local sample initially failed at p95 393.698ms. Prior main also exceeded
300ms in an interleaved comparison. Reducing warm Git processes from seven to five
produced a passing 240.243ms p95; the final combined-code run is also PASS at
274.538ms p95, without weaker journal validation. Earlier 50k and
100k runs still failed the unchanged 10k limits. They are diagnostic historical
measurements, not qualified larger-history budgets. Full replay after journal
changes and repeated append validation remain scaling work.

The captured Windows/Python 3.12 timeout stack is inside derived schema creation.
Its DDL previously committed one statement at a time. The separate 3.13 timeout
had no child stack. Atomic construction addresses a measured cost, not a proof
that all hosted timeouts have one cause. Preserve diagnostics and original limits.

Next useful bounded work is safe incremental materialization/append validation
under PM-012, with explicit accepted budgets, corruption/race/reconstruction
equivalence and 10k/50k/100k evidence. Preserve full-byte tamper detection; do not
replace it with mtime/size trust or a silent weakened cache. Continue the canonical
dependency order in #74: provider adapters/languages and lineage; drift/revalidation;
complete cited context and FR/EN retrieval; why/history and impact; multi-writer;
Portal longitudinal storage, UI and grounded answers; final vision qualification.

The new context source field is additive. IdleProof currently tolerates it; full
typed admission, rendered source preservation and Portal projection are still
separate PM-010/PM-013 work. Git bootstrap does not infer task causality, treat
messages as Proof, traverse every branch or yet search Git history in context.

Release blockers remain final coordinated HUMAN scenarios, supported install/
update/rollback, production commercial authority and channel qualification, and
the resolving public release/tag/consumer gate #80. No release/tag was created.
Do not publish an advisory-unlicensed development runtime as commercial ready.

## Before starting the next mutation

Fetch and inspect actual branches, dirty files, PRs and exact-main status. Reuse
existing implementations and preserve user work. Read relevant acceptance and
journal files before proposing the next bounded unit. Record acceptance first,
reproduce a relevant failure, implement, verify and review the complete change,
then qualify/merge/check fresh main under the existing authorization. Do not
restart completed units or suppress earlier failures to obtain a green label.
